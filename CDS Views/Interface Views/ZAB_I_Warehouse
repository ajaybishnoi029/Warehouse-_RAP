@AccessControl.authorizationCheck: #NOT_REQUIRED
@EndUserText.label: 'Root View for Warehouse'
@Metadata.ignorePropagatedAnnotations: true
define root view entity ZAB_I_Warehouse
  as select from zwm_warehouse
  composition [0..*] of ZAB_I_StorBin as _storbin
  association [1..1] to ZAB_STATUS_VH as _STATUS on _STATUS.Status = $projection.Status
{
  key warehouse_id          as WarehouseId,
      name                  as Name,
      @Search.defaultSearchElement: true
      @Semantics.address.city: true
      city                  as City,
      @Consumption.valueHelpDefinition: [{entity: { name: 'I_CountryVH', element: 'Country' } }]
      @Semantics.address.country: true
      country_code          as CountryCode,
      @Semantics.telephone.type: [#WORK]
      phone_number          as PhoneNumber,
      @Semantics.eMail.address: true
      email_address         as EmailAddress,
      status                as Status,

      @Semantics.user.createdBy: true
      local_created_by      as LocalCreatedBy,
      @Semantics.systemDateTime.createdAt: true
      local_created_at      as LocalCreatedAt,
      @Semantics.user.lastChangedBy: true
      local_last_changed_by as LocalLastChangedBy,
      @Semantics.systemDateTime.localInstanceLastChangedAt: true
      local_last_changed_at as LocalLastChangedAt,
      @Semantics.systemDateTime.lastChangedAt: true
      last_changed_at       as LastChangedAt,
      _storbin,
      _STATUS
}
